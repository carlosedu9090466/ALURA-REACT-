import './Banner.css';

export const Banner = () => {
    // JSX
    return (
        // className para o mesmo criterio de classe em html e css
        <header className="banner">
            <img src="/imagens/banner.png" alt="Banner principal da página do Organo" />
        </header>
    );
}
